class Settings:
    # These parameters should be set to other values after
    # loading the ui default values
    num_ants = 100
    alpha = 3.0
    rho = 0.005
    draw_ants = True
    draw_pheromones = True

settings = Settings()
